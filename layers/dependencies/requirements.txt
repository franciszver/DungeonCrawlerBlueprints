boto3>=1.28.0
requests>=2.31.0
Pillow>=12.0.0
# Note: Shapely, numpy, and opencv-python-headless are too large for Lambda layer (250MB limit)
# These features will gracefully degrade if dependencies are not available:
# - Polygon collision detection falls back to bounding box checks
# - Edge detection uses PIL (Pillow) as default, OpenCV available via container image (optional upgrade)
# To enable OpenCV, use container image deployment (see _docs/DEPLOYMENT_GUIDE.md)

